package org.example;

import java.util.HashMap;

public class Database {
    HashMap<Integer, Salesman> salesman = new HashMap<>();
    HashMap<Integer, Sale> sale = new HashMap<>();
    HashMap<Integer, Product> product = new HashMap<>();
    HashMap<Integer, Salesman> category = new HashMap<>();
}
